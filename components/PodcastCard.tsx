import React from 'react'

const PodcastCard = ({
    imgURL,
    title,
    description,
    podcastId,
}: {
    imgURL: string;
    title: string;
    description: string;
    podcastId: number;
}) => {
  return (
    <div>PodcastCard</div>
  )
}

export default PodcastCard